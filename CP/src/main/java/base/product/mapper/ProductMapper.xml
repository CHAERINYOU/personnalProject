<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="base.product.mapper.ProductMapper">
	
	
	<insert id="create" parameterType="base.product.Product">
			insert into product(productno,productname,productprice,productimage,productdetail,productbrand,productclickcount,productamount) 
			values(#{productno},#{productname},#{productprice},#{productimage},#{productdetail},#{productbrand},#{productclickcount},#{productamount})
	</insert>
	
	
	<update id="update" parameterType="base.product.Product">
		
	</update>
	
	
	<delete id="remove" parameterType="base.product.Product">
	
	</delete>
	
	
	<select id="findboard" parameterType="base.product.Product">
	
	</select>
	
	
	<select id="findBoardList" resultType="base.product.Product">
		
	</select>
	
	
	<!-- <select id="existedUser" 	parameterType="java.lang.String" 
								resultType="java.lang.Integer">
		select count(*) cnt from userinfo where userid=#{userId}
	</select> -->
	
	
</mapper>